<%= turbo_frame_tag article do %>
  <div class="text-slate-200 border-solid border-b py-5">
    <p><%= link_to article.title, article_path(article), data: { turbo_frame: "_top" } %></p>
    <p class="my-5">
      <%= article.content %>
    </p>
  </div>
<% end %>
